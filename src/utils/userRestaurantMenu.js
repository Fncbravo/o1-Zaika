import { useEffect } from 'react';

const userRestaurantMenu = (resId) => {
    // fetch data

    useEffect(() => {
        fetchData();
    }, []);


    return resInfo;
}

export default userRestaurantMenu;